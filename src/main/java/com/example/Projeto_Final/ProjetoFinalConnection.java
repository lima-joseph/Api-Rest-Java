package com.example.Projeto_Final;

import org.springframework.web.servlet.config.annotation.CorsRegistry;

public interface ProjetoFinalConnection {
    void addCorsMappings(CorsRegistry registry);
}
